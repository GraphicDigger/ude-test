/* DEPRECATED (2026-05-28): legacy TS theme.
 *
 * Заменено на CSS-vars (`tokens.css`) + JS wrapper (`sys.ts`, `typography.ts`).
 * Файл оставлен пустым — пользователь может удалить через `git rm` после
 * verify, что в проекте нет import'ов lightTheme/darkTheme.
 */
export {};
